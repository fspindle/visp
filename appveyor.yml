# Specify version format
version: "3.1.0-{build}"

os: Visual Studio 2015

# scripts that are called at very beginning, before repo cloning
init:
  - cmd: cmake --version
  - cmd: msbuild /version

install:
  # All external dependencies are installed in C:\projects\deps
  - mkdir C:\projects\deps
  - cd C:\projects\deps

  # visp-images
  - set VISP_IMAGES_URL="http://visp-doc.inria.fr/download/dataset/ViSP-images-3.0.1.zip"
  - appveyor DownloadFile %VISP_IMAGES_URL% -FileName visp-images.zip
  - 7z x visp-images.zip -oC:\projects\deps\visp-images > nul
  - set VISP_INPUT_IMAGE_PATH=C:\projects\deps\visp-images

# clone directory
clone_folder: C:\projects\visp\visp

build_script:
  - dir C:\projects\deps
  - dir C:\projects\deps\visp-images
  - echo %VISP_INPUT_IMAGE_PATH%
  - md C:\projects\visp\build
  - cd C:\projects\visp\build
#  - cmake -G "Visual Studio 14 2015 Win64" ..\visp -DBUILD_EXAMPLES=OFF -DBUILD_TUTORIALS=OFF -DBUILD_DEMOS=OFF
##  - cmake --build . --config Release --target visp_modules
#  - cmake --build . --config Release
#  - ctest


